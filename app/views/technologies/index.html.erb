<h1>All your technologies</h1>

<% if @technologies.empty? %>
  <p>You don't have any technology yet</p>
<% else %>
  <ul>
    <% @technologies.each do |technology| %>
      <li><%= link_to technology.name, technology_path(technology) %></li>
    <% end %>
  </ul>
<% end %>
<%= link_to "Create new Technologies", new_technology_path %>